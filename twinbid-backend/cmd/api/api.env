# HTTP
HTTP_HOSTNAME=0.0.0.0
HTTP_PORT=8080

# PostgreSQL
POSTGRES_DSN=postgres://taslav:1234@127.0.0.1:5432/twinbid?sslmode=disable

# Auth
JWT_SECRET=change-me-super-secret
ACCESS_TOKEN_TTL=5m
REFRESH_TOKEN_TTL=720h

# Frontend CORS origin
FRONTEND_URL=https://twinbid.io

# S3 (AWS S3 or S3-compatible like MinIO)
S3_ENDPOINT=https://cstore.twinbid.io
AWS_REGION=us-east-1
S3_BUCKET=creatives
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_SESSION_TOKEN=
S3_USE_PATH_STYLE=true
S3_UPLOAD_URL_TTL_SECONDS=900
S3_DOWNLOAD_URL_TTL_SECONDS=900
S3_PRESIGN_TTL=15m

# Email (SMTP) — для регистрации и уведомлений
REGISTRATION_TOKEN_TTL=6h
REGISTRATION_TOKEN_CLEANUP_INTERVAL=10m

SMTP_HOST=mail.hosting.reg.ru
SMTP_PORT=587
SMTP_USERNAME=noreply@twinbidex.com
SMTP_PASSWORD=SDU-jeP-7rf-wCL
SMTP_FROM=noreply@twinbidex.com
SMTP_TLS_TYPE=starttls
LOW_BALANCE_CHECK_INTERVAL=10m

LOW_BALANCE_CHECK_INTERVAL=10m
